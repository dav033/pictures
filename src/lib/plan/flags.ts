export const PLAN_DECORACION_ENABLED = ["1", "true", "on"].includes((process.env.PLAN_DECORACION_ENABLED ?? "").toLowerCase());
export const IMAGE_QA_VISION = ["1", "true", "on"].includes((process.env.IMAGE_QA_VISION ?? "").toLowerCase());
