# @sempertex/happie-package-ia

Paquete del workspace `demo-decoracion`. Código independiente del dominio de decoración.

## Desarrollo

```bash
npm run build --workspace=@sempertex/happie-package-ia
npm run dev --workspace=@sempertex/happie-package-ia
```
