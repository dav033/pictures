# Estado · fase A0 (loop)
Prompt: fase-a-loop 1.0.0 · Rama: fase-a/a0-linea-base · Actualizado: 2026-09-15
Estado global: EN_CURSO

| Id | Estado | Evidencia / notas |
|---|---|---|
| T0 | COMPLETADA | Árbol limpio en `2026-09-14` (HEAD `8590d90`); rama creada; `.gitignore` e inventario revisados (abajo) |
| T1 | COMPLETADA | Parte 3: lista explícita de cajas por defecto (`ejemplo-10/REF_01_E03`, retiro "A7 fusionado") y vectores `box_2d` (fuera de rango, invertidos, área mínima, inválida→caja por defecto F10) en `ia:test-analisis-ejemplos`; `plan:test` exit 0 (182). A0.2 tarea 5 (laboratorio F18) queda en `REVISION` por requerir confirmación de uso. Parte 1: las 8 pruebas de A0.2 pasan en la base (exit 0 cada una, sin `process.env`/`fetch`) y se agregaron a `plan:test`; `npm run plan:test` exit 0 (178 casos). Parte 2 hecha: fixture `eval/fixtures/exif/orientacion-6.jpg` + 2 casos en `ia:test-referencias-ruta` (caracterización: el servidor no normaliza; la UI sí). Falta: lista explícita de cajas por defecto |
| T2 | OMITIDA | Protocolo (sección "Convivencia con el loop recolector", cambio local del usuario detectado 2026-09-15): la hace el loop recolector en `demo-decoracion-recolector` / `datos/recolector-estructuras`; la fase A no escribe en `DATA_ROOT` |
| T3 | OMITIDA | Ídem (recolector) |
| T4 | OMITIDA | Ídem (recolector) |
| T5 | BLOQUEADA | Esperando imágenes aceptadas del recolector: `DATA_ROOT\manifests\snapshots\aceptadas-latest.json` no existe (verificado 2026-09-15) |
| T6 | COMPLETADA | Parte 6: `scripts/migrations/025_precios_gemini_flash.sql` con `gemini-3.6-flash` (dos vigencias, fuente oficial consultada el 2026-09-15), verificada en PGlite y no aplicada. **Queda sin hacer de A0.1:** (a) el código no llena todavía `ai_call_log.coste_estimado`/`pricing_id`, así que el criterio "≥99 % de filas con `coste_estimado`" no se cumple con esta rama; (b) no hay precio de `gemini-3.1-flash-image` (limitación de la clave única); (c) `motor_imagen_previsto` y `clase_rechazo` quedan NULL. Todo está en `REVISION`. Parte 1 hecha: `TurnoChat.finishReason`/`blockReason` opcionales en `agente-core` (`extraerCierreGemini`, `turno` y `turnoStream`); prueba en `gemini-chat.test.ts`; `agente-core:test-gemini-chat` y `-errores` (antes fuera de CI) agregadas a `plan:test`. Parte 2 hecha: `ChatPort.thinkingLevel` efectivo, `EventoLlamadaIA.finishReason`/`configHash`, `analysisConfigHash` (sin tocar la clave de caché, eso es A1.3) y registro por pase; `ia:test-telemetria-referencias` (5 casos, puerto simulado, ausencia de contenido sensible) en `plan:test`. Parte 3 hecha: `scripts/migrations/024_telemetria_plan_a.sql` (aditiva, idempotente, rollback) y el `INSERT` de `ai_call_log` escribe `finish_reason`/`config_hash`; verificada en PGlite desechable; **no aplicada a Neon** (en `REVISION`); `agente-core:test-telemetria` (7 casos) en `plan:test`. Parte 4 hecha: `registrarPlanAudit` escribe `tiene_referencia`, `tiene_foto_espacio`, `lora_mode`, `rechazos_turno` y `superficie` desde `/api/chat` (vía `ejecutar.ts` → registro); `PLAN_NO_CONVERGE` se audita; `motor_imagen_previsto` y `clase_rechazo` quedan NULL a propósito (sin dueño en servidor / clases de A4.1). Los errores de esquema de `confirmar_plan_decoracion` todavía no se auditan: hoy solo actualizan `rag_query_log` a `aclaracion`. Parte 5 hecha: rechazo de esquema auditado como `PLAN_ESQUEMA_INVALIDO` (solo rutas y mensajes del validador) y consulta versionada `scripts/consultas/volumen-turnos-con-foto.sql` (verificada en PGlite; no se corrió contra Neon). Falta: filas de `ai_model_pricing` (hacen falta para el costo estimado de T7/T8; requieren precio con fuente oficial y fecha) |
| T7 | EN_CURSO | Parte 1 hecha: contrato `prediccion-estructuras.v1` (Zod, `src/lib/eval/estructuras/prediccion.ts`) y adaptador del paso 1 de §4.7 (`familia-v1-v2.ts`, temporal, con condición de retiro), más `eval:test-estructuras-prediccion` (5 casos) en `plan:test`. Parte 2 hecha: opción `observarPase` en `analizarReferenciasV2`, solo para evaluación, sin cambio de comportamiento (argumentos crudos por intento; no se llama con caché, análisis fijo ni análisis compartido en vuelo); 2 casos nuevos en `ia:test-telemetria-referencias`. Falta: tabla de precios fechada, núcleo del runner (`--preview` sin red, `--max-usd`, concurrencia ≤4, plazos, reanudación, `run.json`) y CLI. El `--preview` sobre `dev-seed-v0` depende de T5 (bloqueada) |
| T8 | PENDIENTE | Depende de T5, T6, T7 |
| T9 | PENDIENTE | Depende de T8 |

## Inventario inicial (T0, 2026-09-15)

- **CI** (`.github/workflows/checks.yml`): build de workspaces, `contracts:check`, lint, build, `tsc --noEmit`; job con base (`rag:migrate`, `contracts:test*`, `plan:test`, `chat:test-historial`, `happie:test-webhook`, `test-idempotency-store`); job Python (`uv lock --check`, ruff, mypy, `generate_models --check`, pytest).
- **`plan:test`** agrupa 25 scripts. No incluye: `ia:test-referencias-reglas`, `-perceptual`, `-fixture`, `-ruta`, `ui:test-recorte-referencia`, `ui:test-armazon`, `ia:test-generate-qa-plan`, `ia:test-qa-esperados` (entrada de T1).
- **Runner de reconocimiento:** no existe (`scripts/eval/`, `src/lib/eval/`, `tools/` ausentes). `eval/` tiene `chat, expected, fixtures, ia, prompts, rag, results, ui`; sin `eval/estructuras/` ni `eval/fixtures/exif/`.
- **Manifiestos de estructuras:** `datasets/` no existe. `.gitignore` no lo ignora (correcto: la proyección sin datos personales debe versionarse). `DATA_ROOT` está fuera del repo, no requiere regla; aún no existe la carpeta (la crea T2).
- **Telemetría:** `finishReason` no aparece en `packages/agente-core/src` ni en `src/lib/ia` (entrada de T6). Última migración `scripts/migrations/023_embedding_provenance.sql`; `024` libre.
- **Herramientas de imagen previas:** scripts Python con Pillow en `scripts/*.py` (datasets Sempertex); `services/ai-api` usa `uv` con `httpx==0.28.1`. Python 3.14.5 y uv 0.12.6 en la máquina.
- **Cambios locales de `scripts/test-armazon-ui.ts`** que menciona Plan A §A0.2: ya no hay cambios sin commit.

## Contradicciones registradas

- Plan A §A0.3 pide mapear "blueprint → predicción", pero `STRUCTURE_TYPE_MAP` (`src/lib/ia/reference-structure.ts:103-117`) funde los tipos del detector al construir el blueprint (`hoop`→`arco`, `ceiling_installation`→`guirnalda`, `sculpture`/`bouquet`/`cluster`→`kit`), y el paso 1 de Fundamentos §4.7 necesita el tipo original. Interpretación conservadora: el runner mapea desde la salida cruda del inventario y la auditoría, parseada con el mismo `parseDetectedStructure` de producción, y la guarda por hash como pide A0.3.

- Plan A §A0.2 pide que "la prueba EXIF pase o exista un PR con normalización", pero normalizar en `analisis-http.ts` cambia producción (fuera de esta corrida). Verificado: `src/app/page.tsx` (`redimensionarImagen`) y `src/app/laboratorio-referencias/page.tsx` enderezan en el navegador (`createImageBitmap` con `imageOrientation: "from-image"` + recodificación en canvas); el servidor reenvía los bytes intactos. Se dejó una prueba de caracterización con condición de retiro y la decisión en `REVISION`. No se sabe si Gemini aplica la orientación EXIF (comprobarlo cuesta una llamada paga; no se hizo).

- Guía §5.3 ubica los scripts de adquisición en `tools/dataset-estructuras-ext/` [P] (Python con dependencias bloqueadas) y Plan A §A0.3 ubica métricas en `tools/eval-estructuras/`; ninguna existe. Interpretación conservadora: crear `tools/dataset-estructuras-ext/` como proyecto `uv` propio con `uv.lock`, sin mezclar dependencias con `services/ai-api`.

## Conteos
Candidatas por clase (licencia verificada): 0 · descargas usadas: 0 / 800 · cuarentenas: 0

## Gasto
Gemini estimado acumulado: US$0 · reportado: US$0 · tope: US$15

## Bitácora (más reciente arriba)
- 2026-09-15 T7 (parte 2) · gancho `observarPase`. Restricciones para el runner, verificadas en el código: hay que pasar `forzarNuevoAnalisis` y no correr la misma imagen en paralelo, porque `enVuelo` comparte la llamada aun forzando. Un error del observador se propaga y no provoca reintento · verificación: `ia:test-telemetria-referencias` exit 0 (7 casos), `ia:test-referencias-reglas`, `-perceptual` e `ia:test-analisis-ejemplos` exit 0, `npm run lint` exit 0 (30 warnings = base), `npx tsc --noEmit` exit 0, build de workspaces exit 0, `npm run plan:test` exit 0 · siguiente: tabla de precios fechada `eval/estructuras/precios/2026-09-15.json` y estimador de costo de `--preview`.
- 2026-09-15 T7 (parte 1) · contrato de predicciones y adaptador de familia. Contradicción registrada (abajo): el blueprint v2 no conserva el tipo del detector, por eso el runner mapeará desde la salida cruda parseada con `parseDetectedStructure` · verificación: `npx tsx scripts/test-eval-estructuras-prediccion.ts` exit 0 (5 casos), `npm run lint` exit 0 (30 warnings = base), `npx tsc --noEmit` exit 0, build de workspaces exit 0, `npm run plan:test` exit 0 · siguiente: gancho de observación de las salidas crudas en `analizarReferenciasV2` y tabla de precios fechada.
- 2026-09-15 T6 (parte 6, cierre) · precios de `gemini-3.6-flash` verificados en https://ai.google.dev/gemini-api/docs/pricing ("Last updated 2026-09-15 UTC"): estándar hasta 2026-12-31 entrada US$0,75/M, salida US$3,75/M, caché US$0,075/M; desde 2027-01-01 el doble. Coinciden con Fundamentos §8.6. Migración `025` de datos · verificación: script en PGlite (011/013/021/024/025, 025 dos veces) → `[PASS]` (vigencias sin solape en 2026-12-31/2027-01-01, costo de ejemplo con `calcularCosteEstimado`, rollback respeta `ON DELETE RESTRICT`); `git diff --check` limpio; sin cambios de TS (no se corrieron lint/tsc/build) · siguiente: T7 (runner de reconocimiento con `--preview`). Nota para T7: Fundamentos §8.6 pide una tabla de precios fechada en `eval/estructuras/precios/<fecha>.json` que cite la misma fuente.
- 2026-09-15 T6 (parte 5) · `PLAN_ESQUEMA_INVALIDO` en `plan_audit_log` y consulta de volumen semanal. Hallazgos: (1) no existe marca de tráfico E2E ni local (los smoke usan UUID aleatorios y un servidor local escribe en la misma base), así que la consulta solo filtra por superficie y da una cota superior (decisión en `REVISION`); (2) en `plan_audit_log`, `request_id` es el `ragRequestId` de la conversación, no del turno, y por eso los turnos se cuentan en `ai_call_log`; (3) `scripts/rag-metrics.ts` usa `COUNT(*)` de `plan_audit_log` como total: las filas nuevas de rechazo lo aumentan, igual que los rechazos que ya se auditaban · verificación: `plan:test-tercera-e2e` exit 0 (11 casos), consulta sobre PGlite con filas sintéticas y dentro de `READ ONLY` → `[PASS]`, `npm run lint` exit 0 (30 warnings = base), `npx tsc --noEmit` exit 0, build de workspaces exit 0, `npm run plan:test` exit 0 · siguiente: precios de `ai_model_pricing` con fuente oficial (entrada del costo estimado de T7).
- 2026-09-15 T6 (parte 4) · hechos de la petición en `plan_audit_log` y auditoría de `PLAN_NO_CONVERGE`. Escribir `motor_imagen_previsto` exigiría duplicar en el servidor la regla de `src/lib/estado/modo-vista-reglas.ts` (dos dueños, contra `AGENTS.md`): queda NULL y anotado. `DATA_ROOT` ya tiene la estructura del recolector (12:34), pero sin `aceptadas-latest.json`: T5 sigue bloqueada · verificación: `plan:test-tercera-e2e` exit 0 (10 casos, 1 nuevo con pool simulado), `INSERT` real de `registrarPlanAudit` contra PGlite con 011/013/021/024 → `[PASS]` (script temporal borrado), `npm run lint` exit 0 (30 warnings = base), build de workspaces exit 0, `npx tsc --noEmit` exit 0, `npm run plan:test` exit 0, `contracts:test`, `contracts:test:domain` y `chat:test-historial` exit 0 · siguiente: auditar los errores de esquema de `confirmar_plan_decoracion` y la consulta de volumen.
- 2026-09-15 T6 (parte 3) · migración `024` + `INSERT` durable con las columnas nuevas. Base desechable: no hay Postgres local y Docker no corre; se usó PGlite (Postgres en WASM) instalado solo en el scratchpad, sin dependencia nueva en el repo. Script de verificación fuera del repo: aplica 011→013→021→024, 024 dos veces (idempotente), filas previas con NULL, `INSERT` real de `agente-core` compilado, 4 restricciones rechazan, rollback documentado revierte sin borrar filas y 024 se re-aplica → `[PASS]`. En CI, `rag:migrate` la aplicará sobre Postgres real (no verificado aquí) · verificación local: `npm run lint` exit 0 (30 warnings = base), build de workspaces exit 0, `npx tsc --noEmit` exit 0, `npm run plan:test` exit 0, `npm run contracts:check` exit 0 · protocolo: el usuario agregó (sin commit) la convivencia con el loop de la fase C (`demo-decoracion-fase-c`, puerto 8090; `run.json` sin datos personales) · siguiente: columnas de `plan_audit_log` desde la auditoría de planes y `PLAN_NO_CONVERGE`.
- 2026-09-15 T6 (parte 2) · telemetría por pase del análisis de referencias (`finish_reason`, `thinking_level` efectivo, `prompt_version`, `config_hash`) en el evento, sin cambiar SQL: `deploy.yml` no corre migraciones y un `INSERT` con columnas inexistentes perdería toda la telemetría en silencio · verificación: `npm run lint` exit 0 (30 warnings = base), `npm run build --workspaces --if-present` exit 0, `npx tsc --noEmit` exit 0, `npm run plan:test` exit 0, `ia:test-referencias-reglas` y `contracts:test:cancel` exit 0 · siguiente: migración `024` (con rollback) + `INSERT` de `finish_reason`/`config_hash`, probada en base desechable.
- 2026-09-15 T6 (parte 1) · `finishReason`/`blockReason` en el puerto Gemini (campo opcional, sin cambio de comportamiento) · verificación: `npm run lint` exit 0 (0 errores, 30 warnings idénticos a la base), `npm run build --workspaces --if-present` exit 0, `npx tsc --noEmit` exit 0, `npm run plan:test` exit 0 (incluye las 2 pruebas de agente-core: 4 + 4 casos) · siguiente: telemetría por pase en el análisis de referencias.
- 2026-09-15 Protocolo actualizado por el usuario (sin commit en `prompts/fase-a-loop.md`; no lo commitea este loop): T2–T4 pasan al recolector, T5 bloqueada hasta que exista la instantánea `aceptadas-latest.json`; `show-toplevel` y rama verificados · siguiente: T6 (A0.1 instrumentación).
- 2026-09-15 T1 (parte 3, cierre) · 1 caja por defecto en el análisis fijo (73 cajas revisadas), lista de excepciones exacta; 6 vectores `box_2d` vía `analizarReferenciasV2` con puerto simulado · verificación: `npm run ia:test-analisis-ejemplos` exit 0 (7 casos), `npx eslint` del archivo exit 0, `npx tsc --noEmit` exit 0, `npm run plan:test` exit 0 (182 casos) · siguiente: T2 (herramienta de recolección Commons, `--dry-run` por defecto).
- 2026-09-15 T1 (parte 2) · fixture EXIF orientación 6 (sintético, 1,2 KB) con README; 2 casos en `scripts/test-reference-analyze-route.ts` · verificación: `npm run ia:test-referencias-ruta` exit 0 (12 casos), `npx eslint` del archivo exit 0, `npx tsc --noEmit` exit 0; no se corrió `npm run lint` completo ni build (sin cambios de app ni paquetes) · siguiente: lista explícita de excepciones de cajas por defecto (E03 del análisis fijo) con condición de retiro "A7 fusionado"; con eso T1 queda completa.
- 2026-09-15 T1 (parte 1) · 8 pruebas fuera de CI corridas en la base: todas exit 0; agregadas al final de `plan:test` · verificación: `npm run plan:test` exit 0; solo cambia `package.json` (sin TS nuevo, no se corrió lint/tsc/build) · siguiente: fixture EXIF orientación 6 en `eval/fixtures/exif/` contra `/api/references/analyze` con puerto simulado, luego lista de excepciones de cajas por defecto.
- 2026-09-15 T0 · rama `fase-a/a0-linea-base` creada desde `2026-09-14` limpio; ESTADO y REVISION creados; inventario · verificación: `git status` limpio antes de crear la rama; solo documentación (sin checks de aplicación, según `AGENTS.md`) · siguiente: T1 (correr en la base las 8 pruebas fuera de CI).
