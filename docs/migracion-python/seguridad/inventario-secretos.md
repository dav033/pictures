# Inventario de secretos

Inventario basado en el código del repositorio y en los nombres de variables
presentes en `.env.local`. No se incluye ningún valor de secreto.

| Secreto | Dónde vive | Qué protege / qué puede hacer un atacante con él | Quién lo rota hoy | Qué pasa si se filtra | Blast radius (bajo/medio/alto) |
|---|---|---|---|---|---|
| `GEMINI_API_KEY` | Variable del entorno del servidor, localmente en `.env.local`. Se lee en `src/lib/gemini.ts`, `packages/agente-core/src/gemini/chat.ts`, `src/lib/ia/gemini/imagen.ts`, `src/lib/rag/embeddings.ts`, `src/lib/happie/conversacion-webhook.ts` y `packages/happie-package-ia/src/recomendador.ts`. | Autentica chat con herramientas, generación de imágenes, embeddings RAG y los flujos de recomendación/conversación de Happie frente a Gemini. Un atacante puede usarla para enviar solicitudes arbitrarias compatibles con esos servicios y gastar en la cuenta de Gemini de Sempertex; no hay un límite de gasto conocido en el repositorio. | Sin dueño ni procedimiento de rotación documentado hoy | Solicitudes no autorizadas, consumo de cuota y cargos en la cuenta de Gemini, además de abuso de los modelos y capacidades habilitados para esa cuenta. | alto |
| `FAL_KEY` | Variable del entorno del servidor, localmente en `.env.local`. Se usa en `src/lib/ia/sempertex-lora.ts` para generación LoRA y en `src/app/api/lora/trainings/[id]/start/route.ts` para subir datasets y enviar entrenamientos. | Autoriza llamadas a fal.ai para generar imágenes LoRA de texto o edición, consultar sus colas y resultados, e iniciar entrenamientos LoRA. Un atacante puede gastar en fal.ai y generar contenido arbitrario con esa cuenta. | Sin dueño ni procedimiento de rotación documentado hoy | Generaciones y entrenamientos no autorizados, cargos en fal.ai, abuso de la cuenta y posible envío de datasets controlados por el atacante al flujo de entrenamiento. | alto |
| `APP_PASSWORD` | Variable del entorno del servidor, localmente en `.env.local`. Se lee en `src/proxy.ts`, `src/lib/auth/request.ts` y `src/app/api/login/route.ts`; la cookie de sesión se deriva mediante `src/lib/auth/session.ts`. | Es la barrera global de acceso de la aplicación y la única contraseña compartida; no existen usuarios individuales. Un atacante puede iniciar sesión como el usuario común y usar las rutas protegidas, incluidos flujos de catálogo, planes y proveedores de IA. | Sin dueño ni procedimiento de rotación documentado hoy | Toma de control del acceso de la aplicación para cualquier persona que conozca la contraseña, con capacidad de consumir operaciones protegidas y provocar gasto mediante los proveedores conectados. | alto |
| `DATABASE_URL` | Variable del entorno del servidor, localmente en `.env.local`, apuntando al Postgres remoto de Neon. `src/lib/rag/db.ts` la usa para crear el pool de conexiones; el resto de RAG, planes, webhooks y telemetría accede a la base a través de ese pool. | Contiene las credenciales y el destino de la base que almacena todo el catálogo comercial, precios, planes y telemetría de IA. Un atacante con la cadena de conexión puede intentar conectarse directamente y leer, modificar o borrar los datos accesibles por ese usuario. | Sin dueño ni procedimiento de rotación documentado hoy | Compromiso directo de la base remota, exfiltración o alteración de información comercial y de telemetría, corrupción de planes y posible indisponibilidad de los flujos que dependen del catálogo. | alto |
| `PLAN_APPROVAL_SECRET` | Variable del entorno del servidor, localmente en `.env.local`. `src/lib/plan/aprobacion.ts` la usa como clave HMAC-SHA256; los tokens se crean desde `src/app/api/plan-editar/route.ts` y se verifican antes de generar en `src/app/api/generate/route.ts`. | Protege la autenticidad de la aprobación de un plan, vinculada a `plan_hash`, `requestId` y expiración. Si se filtra, un atacante puede forjar tokens de aprobación válidos y disparar generaciones de imagen pagadas sin una aprobación real del cliente. | Sin dueño ni procedimiento de rotación documentado hoy | Se salta la barrera criptográfica de aprobación y puede provocar generaciones pagadas no autorizadas con planes que el servidor considere válidos. | alto |
| `SHOPIFY_WEBHOOK_SECRET` | Variable del entorno del servidor, localmente en `.env.local`. `src/app/api/rag/webhooks/shopify/route.ts` la pasa a `src/lib/rag/webhooks/verificar.ts` para validar `X-Shopify-Hmac-Sha256`. | Protege la autenticidad de los webhooks recibidos desde Shopify. Un atacante que la obtenga puede firmar solicitudes falsas de creación, actualización o eliminación de productos y hacer que el procesador modifique el catálogo persistido, sujeto a su normalización e idempotencia. | Sin dueño ni procedimiento de rotación documentado hoy | El catálogo RAG puede quedar contaminado, desactualizado o con productos eliminados, afectando búsquedas, recomendaciones, precios, existencias y planes posteriores. | medio |
| `HAPPIA_API_KEY` | Variable del entorno del servidor, localmente en `.env.local`. `packages/happie-package-ia/src/config.ts` la carga y `packages/happie-package-ia/src/cliente.ts` la envía como `x-api-key` al endpoint remoto `${HAPPIA_API_BASE_URL}/packages`. Se usa desde los flujos de opciones y recomendaciones de Happie. | Autentica el acceso saliente de la aplicación al catálogo remoto de paquetes de Happia. En el código observado, el uso concreto es listar `/packages`; un atacante puede reutilizarla para consultar ese recurso o cualquier alcance adicional que el proveedor haya asociado a la credencial, no documentado en este repositorio. | Sin dueño ni procedimiento de rotación documentado hoy | Consultas no autorizadas al catálogo de Happia, exposición de datos de paquetes y consumo o abuso de la cuota de la cuenta según los permisos efectivos de la credencial. | medio |
| `HAPPIE_EXTERNO_API_KEY` | Variable del entorno del servidor, localmente en `.env.local`. `src/lib/happie/cors-externo.ts` la compara con el encabezado `x-api-key` para `/api/happie/recommend-packages` y `/api/happie/recommend-package`. | Autoriza los endpoints de recomendación para consumidores desde navegador. La propia implementación advierte que la clave viaja en JavaScript y es visible para quien controla ese origen; CORS no es una barrera contra un cliente que reutilice la clave fuera del navegador. Un atacante puede invocar esos endpoints y hacer que el servidor consulte Happia y Gemini. | Sin dueño ni procedimiento de rotación documentado hoy | Reutilización del servicio externo, generación de recomendaciones no autorizadas, consumo de recursos y posibles cargos de Gemini, además de exposición de las respuestas que entregue el endpoint. | medio |
| `HAPPIE_WEBHOOK_API_KEY` | Variable del entorno del servidor, localmente en `.env.local`. `src/lib/happie/recomendar-paquetes-webhook.ts` la valida con comparación constante para `/api/happie/webhook/recommend-packages` y `/api/happie/webhook/recommend-package`; `src/lib/happie/conversacion-webhook.ts` reutiliza esa autenticación para `/api/happie/webhook/chat`. | Autoriza el flujo server-to-server de Happia para recomendaciones y conversación, separado de la clave externa de navegador. Un atacante puede enviar solicitudes de recomendación o turnos de chat y activar llamadas a Happia y Gemini; el webhook aplica límites de body, timeout, idempotencia y 30 solicitudes por minuto por credencial. | Sin dueño ni procedimiento de rotación documentado hoy | Abuso de los endpoints de integración, consumo no autorizado de Gemini y respuestas de recomendación/conversación para un tercero, dentro de los límites operativos del webhook. | medio |
| Password del rol Postgres `demo_decoracion_ai_api` (Fase 6) | Creado 2026-09-08 directamente en Neon real. Vive solo en el DSN restringido que usa el contenedor del backend Python en el EC2 (`services/ai-api`, variable `DATABASE_URL` de ese servicio); no está en `.env.local` del repo Next ni en ningún archivo del repositorio. | Autentica al servicio Python contra Neon con permisos deliberadamente acotados: `USAGE`/`SELECT`/`INSERT`/`UPDATE`/`DELETE` solo sobre el schema `operational` (`operational_idempotency`, `operational_request_nonces`, `schema_migrations`), `search_path = operational, public`. Verificado con una conexión real que **no** puede leer `public.catalog_products` ("permission denied"). Un atacante con esta credencial no puede leer ni escribir catálogo, precios ni inventario — solo el registro operacional del propio backend Python. | Sin dueño ni procedimiento de rotación documentado hoy (creado en esta sesión; falta decidir quién y cómo la rota) | Acceso de lectura/escritura al registro de idempotencia y nonces del backend Python únicamente; no compromete autoridad comercial por diseño del aislamiento de schema. | bajo-medio |

## Huecos detectados

No se encontró ningún script, runbook ni documento que asigne un dueño o describa un procedimiento de rotación para ninguno de estos secretos:

- `GEMINI_API_KEY`
- `FAL_KEY`
- `APP_PASSWORD`
- `DATABASE_URL`
- `PLAN_APPROVAL_SECRET`
- `SHOPIFY_WEBHOOK_SECRET`
- `HAPPIA_API_KEY`
- `HAPPIE_EXTERNO_API_KEY`
- `HAPPIE_WEBHOOK_API_KEY`
- Password del rol Postgres `demo_decoracion_ai_api` (Fase 6, creado 2026-09-08)
